# GENERATED FILE. DO NOT EDIT! Generated by Reddit BootstrapTask v3
reddit_bootstrap_version=3

# Temporarily fix until we can resolve build cache flakiness
kotlin.incremental.useClasspathSnapshot=false

# Customized JVM args for this machine
org.gradle.jvmargs=-Xmx19g -XX:MaxMetaspaceSize=1g -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8 -Xss5m -XX:+UseParallelGC
kotlin.daemon.jvmargs=-Xmx11g -XX:MaxMetaspaceSize=700m -Xss5m -XX:+UseParallelGC

# Customized worker count for this machine
org.gradle.workers.max=8

org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.configureondemand=true
org.gradle.caching=true
gradle.cache.push=false
org.gradle.unsafe.configuration-cache=true
org.gradle.unsafe.configuration-cache-problems=warn
org.gradle.configuration-cache=true
org.gradle.configuration-cache-problems=warn

android.useAndroidX=true
android.enableJetifier=false
android.nonTransitiveRClass=true

# Temporary workaround to un-break configuration caching.
# https://issuetracker.google.com/issues/162074215
android.includeDependencyInfoInApks=false

# Android plugin 7.0.3 has experimental support for using relative path sensitivity with
# CompileLibraryResourcesTask inputs which will provide more build cache hits
# and improve build speed. Set these configs to enable this support
android.experimental.cacheCompileLibResources=true

# Required for dynamic feature modules
android.experimental.enableNewResourceShrinker=true

# preserves the default values from pre-AGP 8.0
android.defaults.buildfeatures.buildconfig=true
android.nonFinalResIds=false
android.enableR8.fullMode=false

# Test
com.squareup.anvil.trackSourceFiles=true

